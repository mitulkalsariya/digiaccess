{
  "name": "@a11y/github-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that runs an a11y scan against PR preview URLs",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@vercel/ncc": "^0.38.2",
    "typescript": "^5.5.4"
  }
}
